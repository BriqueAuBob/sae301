<div class="relative rounded-xl overflow-hidden">
    {% if homework.picture %}
        <img class="absolute bottom-0 w-full left-O h-full object-cover opacity-10 blur-sm" src="{{ asset('uploads/pictures/' ~ homework.picture) }}" />
    {% endif %}
    <div class="p-4 relative z-10">
        {%if homework.picture %}
            <div>
                <img class="w-full h-40 object-cover mb-2 rounded-xl" src="{{ asset('uploads/pictures/' ~ homework.picture) }}" alt="{{ homework.picture }}">
            </div>
        {% endif %}
        <div class="flex flex-col gap-2">
            <h4 class="font-semibold font-display text-xl">{{ homework.name }}</h4>
            <p class="text-sm">{{ homework.description }}</p>
            <div class="text-sm">
                <img src="{{ asset('images/icons/calendar.svg') }}" alt="calendar" class="inline-block mr-1">
                {{ homework.dueDate|date('d/m/Y à H:i')}}
            </div>
            <div class="text-sm">
                <img src="{{ asset('images/icons/calendar.svg') }}" alt="calendar" class="inline-block mr-1">
                {{ homework.subject.name}}
            </div>
            <div class="text-sm">
                <img src="{{ asset('images/icons/calendar.svg') }}" alt="calendar" class="inline-block mr-1">
                {{ homework.subject.name}}
            </div>
            <div class="text-sm">
                <img src="{{ asset('images/icons/calendar.svg') }}" alt="calendar" class="inline-block mr-1">
                {{ homework.subject.name}}
            </div>
        </div>

    </div>
</div>