<div class="container mx-auto text-center">
    {% if hideIllustration is not defined %}
        <img class="w-1/3 mx-auto mb-4" src="{{ asset('images/search.svg') }}" />
    {% endif %}
    <p class="text-center mx-auto font-medium text-lg max-w-md">{{ text|default('Aucun résultat') }}</p>
</div>