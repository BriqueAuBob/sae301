<span class="bg-white border rounded-2xl px-3 py-1.5 text-sm flex items-center">
    {% if icon is defined %}
        <img src="{{ asset('images/icons/' ~ icon ~ '.svg') }}" alt="{{ icon }}" class="inline-block mr-1 w-4 h-4">
    {% endif %}
    {{ name }}
</span>